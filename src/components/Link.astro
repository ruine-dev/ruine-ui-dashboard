---
import clsx from 'clsx';

const { href, className } = Astro.props;
---
<a href={href} class={clsx('font-semibold text-primary-600 hover:text-primary-700 outline-none focus:ring-2 ring-primary-600 rounded', className)}>
  <slot />
</a>