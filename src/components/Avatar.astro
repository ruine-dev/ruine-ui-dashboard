---
import clsx from 'clsx';
const { src, alt, className } = Astro.props;
---

<img src={src} alt={alt} class={clsx('object-cover w-10 h-10 rounded-full group-focus:ring-2 ring-primary-600 ring-offset-2', className)} />